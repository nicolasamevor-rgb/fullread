[build]
cmd = "composer install && npm install && npm run build"

[start]
cmd = "sh -c 'for i in $(seq 1 30); do php artisan migrate:status > /dev/null 2>&1 && break || sleep 1; done; php artisan migrate --force --seed 2>/dev/null || true; php -S 0.0.0.0:${PORT:-8000} -t public'"
